# 작업 로그 — 2026-07-15

대시보드·분석 탭 완성 → 수신자 상세 고도화(타임라인/상태 모달) → 캠페인 링크별 클릭 + 실행 구간 표시. 커밋 6건 푸시.

## 오늘 한 일 (요약)
1. **대시보드 최근 캠페인 카드화** — 테이블 → 카드 그리드(최신순 6장, 상태 배지·진행률·오픈/클릭 요약). 라인 차트는 `components/ActivityChart.tsx`로 추출해 대시보드/분석 페이지가 공유.
2. **분석 탭 1·2차 + 히트맵** — 스텁이던 페이지를 5구성으로: 기간 선택(7/30/90일) 딸린 **전환 퍼널**(발송→오픈→클릭), 활동 추이 차트, **링크 클릭 Top 10**(전 캠페인 URL 랭킹), **수신자 건강도**(억제 사유 분포 + 리스트별 옵트아웃), **오픈 시간 히트맵**(요일×시각 7×24, Postgres `isodow`/`hour at time zone` 네이티브 집계). 정렬 가능한 캠페인 비교 테이블 포함. 전부 실데이터 브라우저 검증.
3. **수신자 상세 고도화** — Notifuse Contact Details 참고, 우리 모델(이메일+이름만)에 맞게 축소:
   - 이름 수정(`PUT /api/contacts/{id}`) — 이메일은 억제/이벤트가 키로 삼는 식별자라 고정.
   - **활동 타임라인**(`GET /{id}/activity`) — 가입·발송 결과·오픈/클릭·전역 해지·리스트 옵트아웃 8종을 한 줄로 병합, 최신순.
   - **메시지 탭**(`GET /{id}/messages`) — 이 수신자에게 나간 메일과 최종 상태.
   - **리스트 구독 상태 모달** — 리스트별 구독중/해지를 운영자가 변경. 운영자 해지는 `list_unsubscribes`에 reason `"manual"`로 기록(수신자 본인의 `"unsubscribe"`와 구분), 멤버십은 절대 삭제하지 않음.
4. **캠페인 링크별 클릭 + 실행 구간** — Notifuse 캠페인 상세 참고:
   - `GET /api/campaigns/{id}/links` — CLICK 이벤트를 URL별 집계(총 클릭 + distinct 메시지, 많은 순). 상세 페이지에 상대 비율 바·클릭 수신자·클릭율(고유/발송 성공)·총 클릭 테이블, 클릭 없으면 카드 숨김.
   - `campaigns.completed_at`(V12) — COMPLETED 전이시키는 **기존 원자적 `completeIfSending` UPDATE가 시각을 함께 스탬프**(경합 창 없음). 상세 메타에 시작(`enqueued_at`)·완료(`completed_at`)·소요가 떠서 지표 수집 구간이 보임. 레거시 행은 null → 표시 생략.
5. **잡일** — `frontend/tsconfig.tsbuildinfo`를 gitignore + 추적 해제(빌드마다 dirty 방지).

## 상세 (설계 판단 위주)

### 실행 구간 스탬프를 왜 completeIfSending에 넣었나
완료 시각을 따로 UPDATE하면 "완료 판정은 됐는데 시각은 아직" 창이 생긴다. 완료 판정 자체가 `status='SENDING'`에서만 성공하는 조건부 UPDATE이므로 같은 문장에 `completed_at = :now`를 실었다 — 스탬프는 정확히 한 번, 판정과 원자적으로. 포트 시그니처는 그대로 두고 infra 어댑터가 `Instant.now()`를 공급한다.

### 타임라인은 조인 뷰가 아니라 서비스 병합
활동 8종의 출처가 4테이블(mail_messages, email_events, suppressions, list_unsubscribes)이라 SQL 한 방보다 **포트 4개에서 최근 N건씩 모아 서비스에서 정렬·limit**하는 쪽을 택했다. 각 포트는 다른 화면에서도 재사용되고, mail-core가 순수하게 유지된다(테스트도 Mockito만으로).

### 검증
- 데모 캠페인 #44(링크 3개×수신자 6명): pricing 6클릭/4명(66.7%) · docs 2/2 · blog 1/1 — Kafka 프로젝션 경유 집계와 화면 일치, `completed_at` 스탬프 확인.
- 1차 커밋 트리 단독으로 `:mail-core:test` 통과 확인 후 분리 커밋(두 기능이 EmailEvent 포트/어댑터·types.ts를 공유해 hunk 단위로 나눔).

### 커밋 (푸시 완료)
- `2bc54bf` 대시보드 카드 · `5840cfe` 분석 페이지 · `10f34f9` tsbuildinfo
- `dc15c2e` 수신자 상세(이름/타임라인/상태 모달) · `e1c1d01` 링크별 클릭 + 실행 구간(V12)

## 관련 문서
- [04-tracking-analytics.md](../logic/04-tracking-analytics.md)(링크 랭킹·분석 집계·실행 구간 절 추가) · [07-contacts-lists.md](../logic/07-contacts-lists.md)(수신자 상세 절 추가)
