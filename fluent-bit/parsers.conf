# logstash-logback-encoder(LogstashEncoder)가 찍는 필드(@timestamp 등)에 맞춘 JSON 파서.
[PARSER]
    Name          json
    Format        json
    Time_Key      @timestamp
    Time_Format   %Y-%m-%dT%H:%M:%S.%L%z
    Time_Keep     On
