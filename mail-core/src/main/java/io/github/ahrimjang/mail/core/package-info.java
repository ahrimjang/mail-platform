/**
 * mail-core: the domain and business logic of the mail platform.
 *
 * <p>Holds domain models, use-case/service classes, and the ports (interfaces)
 * that infrastructure adapters implement. Depends on {@code mail-common} only;
 * it knows nothing about JPA, the web layer, or specific mail providers.
 */
package io.github.ahrimjang.mail.core;
